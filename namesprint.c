//#include "stdafx.h"
#include <stdlib.h>
#include <conio.h>
#include<stdio.h>
int main()
{
    char a[20];
    int n,i,j,count=0;
    while(count<=3)
    {
    while(scanf("%s",a));
    {
        count++;
        for(i=0;a[i]=='\0';i++)
        {
                a[i] = a[i];
        }
    }
    }
    n = strlen(a);
    for(i=0;i<n;i++)
    {
        printf("%s",a);
    }
    return 0;
}
