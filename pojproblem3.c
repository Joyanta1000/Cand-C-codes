#include<stdio.h>
#include<math.h>
main()
{
    double R, Total;
    int n;
    while(scanf("%lf %d",&R,&n)==2)
          {
              Total = pow(R,n);
                if(Total!=())
              printf("%lf",Total);
          }

}
